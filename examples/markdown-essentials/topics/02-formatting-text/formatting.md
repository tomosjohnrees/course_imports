# Formatting Text

Markdown provides simple syntax for common text formatting:

- **Bold** — wrap text in `**double asterisks**`
- *Italic* — wrap text in `*single asterisks*`
- ~~Strikethrough~~ — wrap text in `~~double tildes~~`

These can be used inline within any paragraph.