# Writing Quizzes

Quizzes let you check whether your learners have understood the material. Quizzes are multiple-choice and are defined as blocks inside a topic's `content.json`, just like text, code, and callout blocks.

A quiz block always has `"type": "quiz"` along with a `"question"`, an `"options"` array, and an `"answer"` index.
