# Step 4: Create the Referenced Files

Notice that the `content.json` above references an external file: `intro.md`. Any block with a `src` field needs a corresponding file in the same topic folder.

Create the `intro.md` file inside `topics/01-what-is-git/`. Here is what it looks like.
