# Step 5: Create the Second Topic

The process for the second topic is identical: create a `content.json` and any referenced files inside the `02-making-commits` folder.

This topic uses text blocks, code blocks, a callout, and a quiz. Here is the complete `content.json` for the second topic.
