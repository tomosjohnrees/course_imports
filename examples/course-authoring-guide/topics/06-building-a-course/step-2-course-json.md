# Step 2: Write course.json

With the folder structure in place, the next step is to create the `course.json` metadata file in the root of the course folder. This file tells Course Imports the course's identity, who wrote it, and which topics to load (and in what order).

Remember, every entry in the `topicOrder` array must match a folder name inside `topics/` exactly. Here is the complete `course.json` for our Intro to Git course.
