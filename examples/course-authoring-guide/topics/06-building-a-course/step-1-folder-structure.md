# Step 1: Create the Folder Structure

Every course starts the same way: create a root folder, add a `topics/` directory inside it, and then create a numbered subfolder for each topic.

For our Intro to Git course with two topics, run the following commands in your terminal.
