// GitHub-specific IPC handlers — populated in issue #0016

export function registerGithubHandlers(): void {
  // No GitHub-specific IPC channels yet.
  // The course:loadFromGitHub handler lives in course.handlers.ts
  // and will delegate to the GitHub client once implemented.
}
